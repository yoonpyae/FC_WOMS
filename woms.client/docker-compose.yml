version: '3.8'

services:
  web:
    build: .
    ports:
      - "5004:80"      # host 2007 -> container 80
    restart: unless-stopped
    networks:
      - webnet

networks:
  webnet:
    driver: bridge