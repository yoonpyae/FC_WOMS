version: '3.8'

services:
  web:
    build: .
    ports:
      - "6008:6008"      # host 6008 -> container 80
    restart: unless-stopped
    networks:
      - webnet
  
networks:
  webnet:
    driver: bridge